{
  "name": "bitty-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "scripts": {
    "dev": "ASTRO_TELEMETRY_DISABLED=1 astro dev",
    "format": "prettier --write README.md package.json tsconfig.json astro.config.mjs wrangler.jsonc .prettierrc.json \".github/workflows/*.yml\" \"src/**/*.astro\" \"src/**/*.ts\" \"src/**/*.css\" \"scripts/**/*.mjs\"",
    "format:check": "prettier --check README.md package.json tsconfig.json astro.config.mjs wrangler.jsonc .prettierrc.json \".github/workflows/*.yml\" \"src/**/*.astro\" \"src/**/*.ts\" \"src/**/*.css\" \"scripts/**/*.mjs\"",
    "lint:md": "markdownlint-cli2",
    "test": "bun test src/lib",
    "typecheck": "ASTRO_TELEMETRY_DISABLED=1 astro sync && tsc --noEmit --project tsconfig.json",
    "build": "ASTRO_TELEMETRY_DISABLED=1 astro build",
    "validate:dist": "test -f dist/index.html && test ! -e dist/_worker.js && test -s dist/_redirects && test -s dist/redirects.json",
    "wrangler:dry-run": "bash -euo pipefail -c 'repo_root=$(dirname \"$(git rev-parse --path-format=absolute --git-common-dir)\"); scratch=\"$repo_root/../recording/bitty-website\"; mkdir -p \"$scratch/wrangler-dry-run\"; log=\"$scratch/wrangler-dry-run.log\"; timeout --signal=INT --kill-after=5s --preserve-status 15s env WRANGLER_SEND_METRICS=false wrangler deploy --dry-run --outdir \"$scratch/wrangler-dry-run\" 2>&1 | tee \"$log\"; grep -Fq -- \"--dry-run: exiting now.\" \"$log\"; grep -Fq -- \"No bindings found.\" \"$log\"'",
    "check": "bun run format:check && bun run lint:md && bun run test && bun run docs:check && bun run typecheck && bun run build && bun run validate:dist && bun run wrangler:dry-run",
    "deploy": "WRANGLER_SEND_METRICS=false wrangler deploy",
    "sync:docs": "bun scripts/sync-docs.mjs",
    "docs:check": "bun scripts/check-docs-sync.mjs"
  },
  "devDependencies": {
    "@astrojs/markdown-satteri": "0.4.0",
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "astro": "7.3.1",
    "bun-types": "1.4.0",
    "carryctx": "0.11.2",
    "github-slugger": "2.0.0",
    "markdownlint-cli2": "0.23.2",
    "prettier": "3.9.6",
    "prettier-plugin-astro": "0.14.1",
    "typescript": "7.0.2",
    "wrangler": "4.129.1"
  }
}
